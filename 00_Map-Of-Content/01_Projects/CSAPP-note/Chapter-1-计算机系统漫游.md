---
tags:
  - Knowledge/computer-science
  - Knowledge/computer-science/CSAPP
created: 2025-08-19
author:
  - ln1
status: Done
---
## 1. 信息就是位+上下文
基本思想：**系统中的所有信息──包括磁盘文件、内存中的程序、存放的用户数据以及网络上传送的数据，都是由一串比特表示的。**
计算机是`01`的世界。
### 选择C语言，它的优势：
- C语言**和Unix操作系统关系密切**；
- C语言**小**而且**简单**；
- C语言是为实践而设计的；
------
### 它的缺点：
- C语言的**指针**是造成程序员困惑和程序错误的常见原因；
- C语言**缺乏**有用的**抽象**的**显式支持**（类、对象、异常）；
## 2. 程序被其他程序翻译成不同的格式：
**每条高级语言语句都必须被其他程序转化位一系列的低级机器语言指令。**

### 汇编语言是非常有用的：
因为它位不同高级语言的不同编译器提供了通用的输出语言。

---
## 3. 了解编译系统的好处：
- 优化程序性能
- 理解链接时出现的错误
- 避免安全漏洞

---
## 4.1 系统的硬件组成
1. 总线：贯穿整个系统的是一组电子管道，称作总线。
2. I/O设备：系统与外部世界的联系通道。
3. 主存：临时存储设备，由动态随机存取存储器（DRAM)芯片组成。
4. 处理器：中央处理单元（CPU）简称处理器，其核心是**程序计数器（PC）**.
	- 在任何时刻，PC都指向主存中的某条机器语言指令，一般是==下一条指令的地址==

---
## 4.2 运行hello程序
![[Pasted image 20250910183029.png]]
![[Pasted image 20250910183041.png]]
![[Pasted image 20250910183103.png]]

---
## 5. 高速缓存非常重要：
问题：
> 根据机械原理，较大的存储设备要比比较小的存储设备运行得慢，而高速设备的造价远高于同类的低速设备。

	意识到高速缓存存储器存在的应用程序员能够利用高速缓存将程序的性能提高一个数量级。

---
## 6. 存储设备形成层次结构：
主要思想：
	**上一层的存储器作为低一层存储器的高速缓存。**
![[Pasted image 20250910182953.png]]

---
## 7. 操作系统（OS）管理硬件
基本功能：
1. 防止硬件被失控的应用程序滥用；
2. 想应用程序提供简单一致的机制来控制复杂而又通常大不相同的低级硬件设备。*可移植性*
### 7. 1.进程
是OS对**正在运行的程序**的一种抽象。在一个系统上可以同时运行多个进程，而每个进程都好像在独占地使用硬件（虚拟）。

上下文：操作系统保持跟踪进程运行所需的所有状态信息，这种状态，就是上下文。

### 7.2.线程
- 一个进程实际上可以由多个称为线程的执行单元组成
- 多线程之间比多进程之间**更容易共享数据**
### 7.3. 虚拟内存
一个抽象概念：每个进程都在独占地使用主存。
每个进程看到的内存都是一致的，称为虚拟地址空间。
![[Pasted image 20250910181817.png]]
- 堆：当调用malloc和free这样的C标准库函数时，堆可以在运行时动态地扩展和收缩。
- 栈：用于实现函数调用，可以动态扩展和收缩。
### 7.4.文件
文件就是字节序列：每个I/O设备，包括磁盘、键盘、显示器，甚至网络，都可以看成是文件。

---
## 8.系统之间利用网络通信
从一个单独的系统来看，系统可视为一个I/O设备。

---
## 9.重要主题
### 9.1.Amdahl定律
- 当我们对系统的某个部分加速时，其对系统整体性能的影响取决于该部分的**重要性**和加速程度。
- 要想显著加速整个系统，必须提升全系统中**相当大**的系统的速度。
### 9.2.并发和并行
两个需求：
- 我们想要计算机做得更多；
- 我们想要计算机运行得更快；
方法：
- 通过并发（concurrency）来让处理器同时做更多的事情，并发指的是一个**同时具有多个活动**的系统。
- 并行（parallelism）：指的是用并发来使一个系统运行得更快。
并行可以在多个抽象层次上运用：
1. 线程级并发：设计出同时有有多个程序执行的系统
	- 单处理器系统
	- 多处理器系统
	- 超线程：同时多线程（simultaneous multi-threading），允许一个CPU执行多个控制流的技术。
2. 指令级并行：现代处理器可以同时执行多条指令的属性被称为指令级并行。
	- 流水线（pipelining）
	- 超标量（super-scalar）：处理器可以达到比一个周期一条指令更快的执行速率
3. 单指令、多数据并行
### 9.2.计算机系统中抽象的重要性
- 指令集架构提供了对实际处理器硬件的抽象
- 文件是对I/O设备的抽象
- 虚拟内存是对程序存储器的抽象
- 进程是对一个正在运行的程序的抽象
- 虚拟机是对整个计算机的抽象：操作系统、处理器和程序
## 10.总结
- 计算机系统由**硬件**和**系统软件**组成。
- 计算机的信息被表示为一组组的**位**，根据**上下文**有不同的解释方式。
- 程序开始是ASCII文本，然后被编译器和链接器编译成二进制可执行文件。
- 处理器读取并解释存放在主存里的二进制指令
- 更高层的存储设备比底层的存储设备要更快，单位比特造价也更高。
- 操作系统是应用程序和硬件之间的媒介，提供三个基本的抽象：
	1. 文件是对I/O设备的抽象
	2. 虚拟内存是对主存和磁盘的抽象
	3. 进程是处理器、主存和I/O设备的抽象
- 从特殊系统的角度来看，网络是一种I/O设备。