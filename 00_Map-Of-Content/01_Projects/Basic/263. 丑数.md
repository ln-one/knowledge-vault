---
tags:
  - Knowledge/Algorithm
  - Knowledge/Programming-Languages/Python
created: 2025-11-06
author:
  - ln1
status: Done
---
## Description
**丑数** 就是只包含质因数 `2`、`3` 和 `5` 的 _正_ 整数。
给你一个整数 `n` ，请你判断 `n` 是否为 **丑数** 。如果是，返回 `true` ；否则，返回 `false` 。
### Example:  
Input: 
```json
n = 6
```

Output: 
```json
true
```
## Question Link

[LeetCode](https://leetcode.cn/problems/ugly-number/)
## Approach

- 先判断是否可以被 2 3 5 整除，如果可以被整除，然后一直除
- 直到不能被除了为止，然后，判断最后的数是否为1即可。

## Implementation

```python
class Solution:
    def isUgly(self, n: int) -> bool:
        if n <= 0:
            return False
        for p in [2,3,5]:
            while n % p == 0:
                n //= p
        return n == 1
```

## Analysis

- [[P1835-素数密度]]
- [[P3383-模板-线性筛素数]]
- [[P2651-添加括号III]]
